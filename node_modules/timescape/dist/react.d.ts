import { DateType, Options } from './index.js';
export { $NOW } from './index.js';
import { MutableRefObject } from 'react';

type TimescapeOptions = {
    date?: Date;
    onChangeDate?: (nextDate: Date | undefined) => void;
} & Options;
declare const useTimescape: ({ date, minDate, maxDate, hour12, wrapAround, digits, onChangeDate, }: TimescapeOptions) => {
    readonly getInputProps: (type: DateType, opts?: {
        ref?: MutableRefObject<HTMLInputElement | null>;
        autofocus?: boolean;
    }) => {
        ref: (element: HTMLInputElement | null) => void;
    };
    readonly getRootProps: () => {
        ref: (element: HTMLElement | null) => void | null;
    };
};

export { DateType, useTimescape };
