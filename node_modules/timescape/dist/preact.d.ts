import { DateType, Options } from './index.js';
export { $NOW } from './index.js';
import { MutableRef } from 'preact/hooks';
import { Signal } from '@preact/signals';

type UseTimescapeOptions = Signal<{
    date?: Date;
} & Options>;
declare const useTimescape: (options: UseTimescapeOptions) => {
    readonly getInputProps: (type: DateType, opts?: {
        ref?: MutableRef<HTMLInputElement | null>;
        autofocus?: boolean;
    }) => {
        ref: (element: HTMLInputElement | null) => void;
    };
    readonly getRootProps: () => {
        ref: (element: HTMLElement | null) => void | null;
    };
};

export { DateType, useTimescape };
