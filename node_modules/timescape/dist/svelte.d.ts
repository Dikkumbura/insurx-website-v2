import { Options, DateType } from './index.js';
export { $NOW as NOW } from './index.js';
import { Writable } from 'svelte/store';

type CreateTimescapeOptions = Writable<{
    date?: Date;
} & Options>;
declare const createTimescape: (options: CreateTimescapeOptions) => {
    readonly inputProps: (element: HTMLInputElement, type: DateType) => HTMLInputElement | undefined;
    readonly rootProps: (element: HTMLElement) => void;
};

export { CreateTimescapeOptions, DateType, createTimescape };
